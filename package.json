{
  "name": "site",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "tinacms dev -c \"npx serve\"",
    "build": "tsc && tinacms build && npm run build:content && npm run build:sitemap",
    "build:content": "node scripts/build-content.js",
    "build:sitemap": "node scripts/build-sitemap.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tinacms/cli": "^1.6.12",
    "gray-matter": "^4.0.3",
    "marked": "^5.1.2",
    "tinacms": "^2.5.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^22.10.1"
  }
}
